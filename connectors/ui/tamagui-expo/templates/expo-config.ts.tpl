/**
 * Tamagui Expo Configuration
 * 
 * Expo/React Native-specific configuration for Tamagui
 */

import { config } from './config';

// Ensure Tamagui is configured for React Native
export const tamaguiConfig = config;

// Export for use in Expo
export default tamaguiConfig;

